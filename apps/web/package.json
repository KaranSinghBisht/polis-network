{
  "name": "@polis/web",
  "version": "0.0.1",
  "private": true,
  "description": "Polis web — Next.js 15 app rendering the five Claude Design surfaces.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start -p 3000",
    "typecheck": "next typegen && tsc -p tsconfig.json --noEmit",
    "lint": "next typegen && tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "clean": "rm -rf .next out"
  },
  "dependencies": {
    "@upstash/redis": "^1.35.0",
    "jose": "^5.9.0",
    "next": "^15.5.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "viem": "^2.48.4"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15",
    "typescript": "^5.5.4"
  }
}
